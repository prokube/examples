apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: workspace-runbooks-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---  # yamllint disable-line rule:document-start
apiVersion: toolhive.stacklok.dev/v1beta1
kind: MCPServer
metadata:
  name: workspace-runbooks
spec:
  image: europe-west3-docker.pkg.dev/prokube-internal/prokube-customer/workspace-runbooks-mcp:latest
  transport: stdio
  proxyPort: 8080
  resources:
    requests:
      cpu: 50m
      memory: 128Mi
    limits:
      cpu: 250m
      memory: 256Mi
  podTemplateSpec:
    spec:
      securityContext:
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
      volumes:
      - name: runbooks
        persistentVolumeClaim:
          claimName: workspace-runbooks-data
      - name: tmp
        emptyDir: {}
      containers:
      - name: mcp
        volumeMounts:
        - name: runbooks
          mountPath: /data
        - name: tmp
          mountPath: /tmp
        securityContext:
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: true
          runAsNonRoot: true
          runAsUser: 10001
          capabilities:
            drop:
            - ALL
